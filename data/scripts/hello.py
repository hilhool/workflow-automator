"""Пример пользовательского скрипта. Всё, что напечатано в stdout, уйдёт в телеграм."""

import sys

date = sys.argv[1] if len(sys.argv) > 1 else "без даты"
print(f"Скрипт отработал. Переданная дата: {date}")
